spring:
  # mvc:
  #   servlet:
  #     path: 
  #Local MongoDB config
  data:
    mongodb:
      database: micro_services
      host: localhost
      port: 27017
  application:
    name: client-gestionar-juegos
    